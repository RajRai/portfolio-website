{
  "name": "portfolio-website",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c auto \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --workspace server run dev",
    "dev:client": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "start": "npm --workspace server run start",
    "lint": "npm --workspace client run lint && npm --workspace server run lint",
    "format": "prettier -w .",

    "docker:build": "docker build -t docker.rajrai.net/portfolio-website:latest .",
    "docker:push": "docker push docker.rajrai.net/portfolio-website:latest",
    "docker:publish": "npm run docker:build && npm run docker:push",
    "docker:start": "docker run --rm -p 5174:5174 -e ADMIN_KEY=change-me -e IP_HASH_SALT=change-me-too -e PORT=5174 -e DEV_CLIENT_ORIGIN=http://localhost:5173 docker.rajrai.net/portfolio-website:latest"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "prettier": "^3.3.3"
  }
}
