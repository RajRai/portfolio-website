# Admin key required for moderation endpoints
ADMIN_KEY=change-me

# Optional: used to salt IP hashes. If unset, a default is used.
IP_HASH_SALT=change-me-too

# Server port
PORT=5174

# In dev, allow Vite origin
DEV_CLIENT_ORIGIN=http://localhost:5173
